# Archived experiment recipes

Temporary Python drivers were removed after the investigation. Their last
complete versions are in commit `25303d6` under this directory. Results and
small explanatory patches remain; production builds use `scripts/`.
